public class Main {
    public static void main(String[] args) {
        int maxCapacity = 150;
        int currentVisitors = 135;
        boolean maxCapacityReached = currentVisitors >= maxCapacity;
        System.out.println("Max capacity reached: " + maxCapacityReached);
    }
}
